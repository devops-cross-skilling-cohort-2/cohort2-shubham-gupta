apiVersion: v1
kind: Secret
metadata:
  name: cohort2-app-secret
  namespace: cohort-prod
type: Opaque
data: {}
# Real values are NOT stored here.
# This file documents the Secret's structure only.
# The secret is created/updated by k8s-prod.yml using GitHub Secrets.
# See .github/workflows/k8s-prod.yml — "Create Secret from GitHub Secrets" step.

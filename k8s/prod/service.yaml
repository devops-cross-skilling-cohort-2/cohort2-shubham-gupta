apiVersion: v1
kind: Service
metadata:
  name: cohort2-app-svc
  namespace: cohort-prod
  labels:
    app: cohort2-app
spec:
  selector:
    app: cohort2-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5050
  type: ClusterIP
